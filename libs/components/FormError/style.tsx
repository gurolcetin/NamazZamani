import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginTop: 5,
    alignItems: 'center',
  },
  errorMessage: {
    marginLeft: 5,
  },
});

export default style;
